def load_user(strategy, response, uid, *args, **kwargs):
    """
        we have to include the user in the pipeline.
        we have available the openid (uid)

        returns a dictionary with the django user: {'user': user}
    """
    pass
